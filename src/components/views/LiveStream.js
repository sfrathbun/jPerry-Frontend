import React, {Component} from 'react';
import PropTypes from 'prop-types';

const LiveStream = () => {

    return (
      <div className="LiveStream">
        This is where you can livestream video
      </div>
    )
  };

LiveStream.propTypes = {
}

export default LiveStream;